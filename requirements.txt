# Python dependencies for the Step Type Prediction (EEG) project.
# Pinned to flexible minor versions; bump as needed.

# Core EEG / signal-processing
mne>=1.6
pyvista>=0.43

# Numerical / data
numpy>=1.24
pandas>=2.0
scipy>=1.10

# Plotting
matplotlib>=3.7

# Classical ML
scikit-learn>=1.3
xgboost>=2.0

# Deep learning (used by the LSTM line)
tensorflow>=2.13   # Keras 2 lives here; if using Keras 3 / PyTorch backend,
                   # replace with `keras>=3` + a backend
# torch>=2.1       # uncomment if any script switches to PyTorch

# Explainability
shap>=0.44
