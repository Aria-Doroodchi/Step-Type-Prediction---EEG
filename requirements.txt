# Python dependencies for the Step Type Prediction (EEG) project.
# Pinned to flexible minor versions; bump as needed.
#
# For development install: pip install -e .[dev,lstm]

# Core EEG / signal-processing
mne>=1.6
pyvista>=0.43

# Numerical / data
numpy>=1.24
pandas>=2.0
scipy>=1.10
pyarrow>=14.0          # parquet for cached feature matrices

# Plotting
matplotlib>=3.7

# Classical ML
scikit-learn>=1.3
xgboost>=2.0

# Deep learning (LSTM)
tensorflow>=2.13
scikeras>=0.12

# Explainability
shap>=0.44

# Future comparator normalization
# Optional Riemannian comparator: pip install -e .[riemannian]
braindecode>=0.8        # exponential moving standardization for CNN path

# Configuration
pyyaml>=6.0

# Automated preprocessing
pyprep>=0.4            # bad-channel detection
meegkit>=0.1.9         # ZapLine line-noise removal
python-picard>=0.8     # Picard ICA solver for MNE
mne-icalabel>=0.6      # ICA component classification (eye/heart/muscle/etc.)
autoreject>=0.4        # local epoch repair/rejection with CV-tuned thresholds
